import 'package:flutter/widgets.dart';

class BPanel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("B"),
      ),
      color: const Color(0xff333388),
    );
  }
}
